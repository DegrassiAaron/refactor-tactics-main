=== RT3 CONTRIB ===

FROM:            DEV-MAIN:39180
TO:              DEV-LEAD
FEATURE:         issue-79-combat-log-blocked-move
WAVE_ID:         issue-79-combat-log-blocked-move/<n> — NON RISOLTO, nessun WORK-ORDER apre questa wave
CREATED:         2026-09-06 15:18
SUPERSEDES:      none
BASE_SHA:        NON RISOLTO — ricevuto il placeholder letterale `<BASE_SHA_REALE>`
PRODUCED_SHA:    = BASE_SHA (nessun commit; nessuna scrittura di produzione)
WORKTREE:        uncommitted (solo questo contributo)
SEED_SOURCE:     none — nessun RNG introdotto, nessun codice scritto
ASSIGNED_SCOPE:  NON RISOLTO — rinvia a INPUT_HANDOFF, che è a sua volta un placeholder
WRITE_SET:       docs/rt-three-terminals/waves/issue-79-combat-log-blocked-move/contrib/DEV-MAIN-39180-01.md (questo contributo)

Nessun file di produzione toccato. Il write-set contiene esclusivamente questo contributo.

## PREFLIGHT — esito

`RT3_CONTRACT.md` §4 è fail-closed sui campi obbligatori. Tre dei quattro non sono risolti,
e il quarto è dato ma **non risolvibile**.

| Campo §4 | Valore ricevuto | Esito |
|---|---|---|
| `FEATURE` | `issue-79-combat-log-blocked-move` | risolto |
| `BRANCH` | `fix/79-blocked-move-turnlog` | **non risolvibile** — non esiste, né locale né remoto |
| `BASE_SHA` | `<BASE_SHA_REALE>` | **placeholder non risolto** |
| `INPUT_HANDOFF` | `<PATH_ASSEGNAZIONE_DEV_LEAD>` | **placeholder non risolto** |

`ASSIGNED_SCOPE` è definito per rinvio (*«usa esclusivamente lo scope DEV-MAIN scritto
nell'INPUT_HANDOFF»*): un rinvio a un campo non risolto non è uno scope. `WAVE_DEV_MAIN.md`
lo nomina esplicitamente come caso di `BLOCKED / MISSING_INPUT`.

§4 vieta di colmare i campi per inferenza: *«un placeholder risolto per inferenza è un input
inventato»*. Non ho dedotto `BASE_SHA` da `HEAD`, né lo scope dal working tree — che
`WAVE_DEV_MAIN.md` vieta a parte, perché una modifica visibile in `git status` può appartenere
a un'altra istanza.

## PRECONDIZIONI §5 — misurate

```text
git rev-parse --abbrev-ref HEAD  -> main
git rev-parse HEAD               -> 75ab62872f9c4e7f083f3328bb5b1213455b0898
git status --porcelain           -> 0 righe (working tree pulito)
```

Due violazioni indipendenti dal preflight, entrambe già sufficienti a `BLOCKED` per §5:

1. `HEAD` non corrisponde a `BASE_SHA` — `BASE_SHA` non è un valore, è un placeholder;
2. il branch corrente è `main`, non il `BRANCH` dichiarato. `CLAUDE.md` §9 e la regola di
   ruolo vietano `git switch` mentre altre istanze DEV hanno lavoro non integrato, e §4 vieta
   comunque di creare il branch per dedurne l'esistenza. Non ho creato né cambiato branch.

Il working tree è pulito: non ho ereditato modifiche non dichiarate. Questo non sblocca nulla,
lo registro perché è la sola precondizione §5 che risulta soddisfatta.

## FATTI MISURATI — per DEV-LEAD

Read-only, nessuna scrittura. Servono a rendere azionabile `UNBLOCK`, non a ricostruire lo scope.

| Verifica | Comando | Esito |
|---|---|---|
| branch di task | `git branch -a --list '*79*'` | **assente** in locale e su `origin` (dopo `git fetch`) |
| directory di wave | `ls docs/rt-three-terminals/waves/` | **assente** — esistono solo `first-hit-provenance/` e `parsecell-arity/` |
| handoff DEV-LEAD | sweep `*DEVLEAD*` / `*DEV-LEAD*` sul repo | solo `parsecell-arity/RT3-DEVLEAD-{022977f,39f3ec9}.md`; **nessuno per la #79** |
| contributi della wave | `contrib/` della feature | la directory non esisteva: l'ho creata per depositare questo contributo |
| issue #79 | `gh issue view 79` | **esiste**, `OPEN` — «CP 11.3 — Combat log con reason code completi», milestone `v0.1 · Leggibilità`, Epic #25, dipende da **#77** |

La issue esiste e il suo corpo è coerente con il feature-slug ricevuto: la DoD nomina
«percorso bloccato → fermo» come primo dei due fallback da rendere espliciti. Ciò che manca non
è il bersaglio della wave — è l'**ingresso** della wave.

⚠️ La dipendenza dichiarata **#77** non è stata verificata come chiusa: verificarla è
scoping (§8) e appartiene a DEV-LEAD, non a questa istanza.

## IMPLEMENTED

Nessuna implementazione. §4 impone di fermarsi prima dello step 1.

Non ho scritto codice, non ho aperto l'audit dell'owner runtime, non ho compilato il contratto
comportamentale: tutti presuppongono uno `ASSIGNED_SCOPE` risolto. Produrli qui significherebbe
scegliere io il punto di estensione, cioè assumere l'arbitrato che appartiene a DEV-LEAD.

## PUBLIC CONTRACT

`N/A` — nessun contratto comportamentale da fornire senza scope assegnato.

## FILES

Nessun file di produzione. Solo questo contributo.

## TESTS

`N/A` — owner DEV-TEST. Nessun test scritto da questa istanza.

## BUILD

`NOT RUN — dominio VALIDATION (ruolo DEV, motore non occupabile)`.

Nessuna build eseguita, nessun `rt-suite`, nessun Automation Test, nessuno Scenario Harness,
nessun Editor, nessuna sessione PIE. Unreal è rimasto libero per tutta la sessione.

## TURNLOG / REPLAY

Nessuna modifica. Nessun evento TurnLog nuovo, nessun cambio di ordinamento, nessuna variazione
di formato o di versione di replay. Nessuna migrazione proposta.

## NETWORK / PRIVACY

Nessuna modifica. Nessun dato di planning esposto, nessuna proprietà replicata aggiunta o
modificata, nessun nuovo percorso di autorità.

## EDITOR EXPECTATION

`N/A` — nessuna attesa Editor-visible da dichiarare. `PREDICTED — NOT VERIFIED` non si applica
a un contributo senza scritture.

## INTEGRATION REQUIRED

Nessuna integrazione fuori scope da proporre: senza scope assegnato non esiste un dentro e un
fuori. Ciò che serve non è un diff, è l'ingresso della wave — vedi `UNBLOCK`.

## VALIDATION REQUESTED

Nessun gate da richiedere. Non esiste un `PRODUCED_SHA` diverso da `BASE_SHA` su cui misurare.

## RISKS

1. **Rischio di input inventato, evitato.** Il rischio dominante era colmare `BASE_SHA` con
   `HEAD` e lo scope con l'analisi del difetto — quest'ultima è pubblica nei commenti della
   issue #79 ed è sufficientemente dettagliata da rendere l'inferenza *plausibile*. È
   esattamente il caso che §4 chiama input inventato: avrei implementato contro un contratto
   che nessun DEV-LEAD ha emesso, e il `WRITE_SET` risultante avrebbe determinato a valle uno
   scoping §8 sbagliato — sistemi non verificati che nessuno sa di dover verificare.
2. **Directory di wave aperta da un contributo.** Ho creato
   `waves/issue-79-combat-log-blocked-move/contrib/` per depositare questo file. La directory
   non è un `WORK-ORDER` e non apre la wave: se DEV-LEAD assegna un feature-slug diverso, questa
   resta orfana e va rimossa.
3. **Registro workspace non verificato.** `rtstatus` e `rtws -Action verify` non sono
   risolvibili in questa shell. L'identità `MAIN` viene quindi dalle variabili d'ambiente, che
   `CLAUDE.md` §2 dichiara non autorevoli. Ininfluente qui — nessuna operazione MCP, nessun
   authoring asset, che sono comunque dominio EDITOR — ma va detto invece che presunto.

## NOT RUN

| Elemento | Motivo |
|---|---|
| Audit dell'owner runtime | `ASSIGNED_SCOPE` non risolto (§4) |
| Contratto comportamentale | dipende dall'audit |
| Build / `rt-suite` / Automation / Scenario Harness / PIE | dominio VALIDATION; ruolo DEV non occupa Unreal |
| `rtstatus`, `rtws -Action verify` | comandi non risolvibili in questa shell |
| Stato della dipendenza #77 | scoping §8, dominio DEV-LEAD |

Nessuna di queste voci si legge `PASS`. §6: `NOT RUN` non conta come `PASS`.

STATUS:   BLOCKED
REASON:   MISSING_INPUT — FIELDS: BASE_SHA, INPUT_HANDOFF, ASSIGNED_SCOPE (placeholder non
          risolti); BRANCH non risolvibile (`fix/79-blocked-move-turnlog` non esiste). In
          aggiunta, §5: HEAD 75ab6287 è su `main` e non corrisponde ad alcun BASE_SHA dichiarato.
UNBLOCK:  DEV-LEAD emette l'ingresso della wave, su disco, e riassegna. Serve:
          1. `docs/rt-three-terminals/waves/issue-79-combat-log-blocked-move/RT3-DEVLEAD-<sha7>.md`
             con la busta §9 completa — `WAVE_ID` con il suo `<n>`, `BRANCH`, `PARENT_BRANCH`
             (base reale della PR, non `main` per default), `BASE_SHA`, `WRITE_SET`,
             `BINARY_ASSETS` — e i sistemi in scope derivati con §8, senza verdetti;
          2. il branch di task `fix/79-blocked-move-turnlog` creato dal `BASE_SHA` dichiarato,
             perché `HEAD` possa corrispondervi senza che io lo deduca;
          3. `ASSIGNED_SCOPE` come **path espliciti** e `OUT_OF_SCOPE` esplicito, così che il
             `WRITE_SET` che restituirò sia un sottoinsieme verificabile;
          4. l'arbitrato che i punti obbligatori presuppongono già deciso, e che non è
             derivabile dal solo corpo della issue: se `Stayed` e `BlockedByUnit` sono
             alternativi o coesistenti nel TurnLog, e quale owner runtime è il punto di
             estensione previsto.
          Ricevuti questi campi, riparto dallo step 1 con un contributo nuovo
          (`DEV-MAIN-39180-02.md`, `SUPERSEDES: DEV-MAIN-39180-01.md`), come impone la regola
          append-only.
