=== RT3 CONTRIB ===

FROM:            DEV-MAIN:39180
TO:              DEV-LEAD
FEATURE:         issue-79-combat-log-blocked-move
WAVE_ID:         issue-79-combat-log-blocked-move/1
CREATED:         2026-09-06 15:56
SUPERSEDES:      DEV-MAIN-39180-01.md
BASE_SHA:        586ad5942b286fcb655c0080c6578cc592390461
PRODUCED_SHA:    = BASE_SHA — non committo, consolida DEV-LEAD
WORKTREE:        uncommitted
SEED_SOURCE:     none — nessun RNG sul percorso, dal click alla voce
ASSIGNED_SCOPE:  i dieci path di `WORK-ORDER.md` § *DEV-MAIN — ASSIGNED_SCOPE*
WRITE_SET:       Source/RefactorTactics/Unit/RTUnit.h
                 Source/RefactorTactics/Unit/RTUnit.cpp
                 Source/RefactorTactics/Player/RTPlayerController.cpp
                 Source/RefactorTactics/Turn/RTTurnManager.cpp
                 docs/rt-three-terminals/waves/issue-79-combat-log-blocked-move/contrib/DEV-MAIN-39180-02.md (questo contributo)

Quattro file su dieci assegnati. I sei non toccati sono dichiarati in `## FILES`, ciascuno con la ragione:
un write-set che elenca ciò che non è stato scritto manda a verificare sistemi che nessuno ha cambiato,
e uno che tace su ciò che è stato scritto lascia sistemi non verificati che nessuno sa di dover verificare.

## ⚠️ HEAD si è mosso durante la sessione — dichiarato, non corretto

Preflight §5 eseguito all'ingresso: `HEAD` = `586ad594` = `BASE_SHA`, branch `fix/79-blocked-move-turnlog`,
working tree pulito a meno di `RT3-VALIDATION-a59671c.md` non tracciato. ✅ Corrispondenza verificata.

A implementazione in corso, `HEAD` misurava `bcde0be9`: tre commit sopravvenuti da un'altra istanza.

```text
bcde0be9 docs(rt3): il work order #79 risponde ai sette finding, e ne accoglie uno che lo correggeva
a49db5fd docs(rt3): il referto EDITOR della wave #79 e' BLOCKED, e la macchina non c'entra
02a48b58 docs(rt3): il referto VALIDATION della wave #79 e' BLOCKED, e dice a quale stadio
```

Misurato prima di proseguire, invece di dedurlo:

```text
git diff --name-only 586ad594..HEAD  ->  3 file, tutti in docs/rt-three-terminals/waves/
git diff --name-only 586ad594..HEAD | grep -E 'RTUnit|RTPlayerController|RTTurnManager'  ->  vuoto
```

A contributo in scrittura `HEAD` misurava `d9f00507` — un quarto commit, `docs(rt3): la sequenza di
consolidamento diventa vincolante, e rtstatus esce dalla wave`. Verificato: tocca **solo** `WORK-ORDER.md`,
e le sue aggiunte sono di processo — sequenza di consolidamento, gate d'ingresso di `1-F6`, `rtstatus`
spostato al follow-up #2602. ⚠️ **Nessun cambiamento al contratto comportamentale né al mio scope**: il
contributo resta valido come scritto.

Nessun file di codice toccato, nessuna mia modifica assorbita da quei commit: il lavoro è rimasto nel
working tree. Non ho eseguito `rebase`, `reset`, `switch` né `pull`, come il mandato impone.

🔴 **Ma uno dei tre commit ha cambiato il contratto sotto di me, e non è un dettaglio di processo.**
`bcde0be9` riscrive `WORK-ORDER.md` accogliendo il Finding `…/1-F3`: i produttori sono **tre**, non due, e
il mio `ASSIGNED_SCOPE` guadagna il punto 6 — il caso del **bot**. L'implementazione che avevo già scritto
copriva player e harness e **avrebbe mancato il terzo**, con un criterio 4 riscritto apposta per non
accettarlo. Ho riprogettato il consumo prima di chiudere: vedi `## IMPLEMENTED`, punto 3.

Il contributo è quindi scritto contro la revisione `bcde0be9` di `WORK-ORDER.md`, non contro quella di
`BASE_SHA`. Chi lo rilegge deve confrontarlo con quella.

## IMPLEMENTED

Il meccanismo è quello arbitrato da DEV-LEAD: il rifiuto sopravvive fino alla fase `Move`, e la voce nasce
nel percorso runtime canonico — `BuildMoveLog`, poi riscrittura dell'esito, esattamente la forma con cui
`bStoppedByTopology` risolve un blocco che il resolver non può vedere. La semantica di movimento non cambia:
nessuna riga tocca `ResolveHexPaths`, il microstep, la priorità di collisione o `TruncatePathTo*`.

**1. Lo stato per-unità** — `Unit/RTUnit.h`, due campi accanto a `PlannedWaypoints`.

```cpp
UPROPERTY(Transient) bool bMovePlanRejectedByOccupant = false;
UPROPERTY(Transient) FRTCellId RejectedMoveDestination;
```

Flag separato dal valore per la ragione di `bDeclaresPlannedFacing`, citata nel WORK-ORDER e verificata sul
posto: la propria cella di partenza **è** una destinazione legittima, quindi nessun `FRTCellId` può fare da
«non dichiarato». `Transient` e non `Replicated`: vedi `## NETWORK / PRIVACY`.

Scelta fra le due opzioni che l'arbitrato 3 lasciava a questo ruolo — campo su `ARTUnit` contro estensione
di `FRTPlannedMovement`. **`ARTUnit`, e non è una preferenza**: `FRTPlannedMovement` è costruito *dentro*
`ResolveMovement` (`RTTurnManager.cpp:7167`, `PlannedMoves.Init(...)`) e muore con la funzione, mentre il
dato deve nascere in `Planning` e sopravvivere al commit. Il canale del `#2314` porta ciò che il resolver
non può ricostruire **nello stesso turno**; qui serve ciò che non sopravvive **fra due fasi**, ed è il
requisito che `ARTUnit` già soddisfa per ogni altro pezzo del piano. Nessun subsystem nuovo, nessun secondo
store: il dato sta dove stanno `PlannedWaypoints`, `PlannedPath` e `PlannedFacing`.

**2. La scrittura, una sola funzione per due chiamanti** — `ARTUnit::NoteMovePlanRejection(Snapshot, UnitId, RequestedCell)`.

Delega la classificazione a `URTHexSimLibrary::ClassifyWaypointCell`: **nessun secondo vocabolario**, e
`Occupied` è l'unico caso che accende il flag. Budget, `BlocksMovement` e `NotOnMap` restano `Stayed`, che è
lo scope dichiarato della #79 e non una svista.

⚠️ **L'assegnazione è incondizionata**, non un `if (bByOccupant)`. È la regola «l'ultimo tentativo governa»:
un rifiuto per occupazione seguito da un rifiuto fuori mappa **spegne** il flag, invece di lasciare acceso un
rifiuto che non è più l'ultimo esito vero. È una decisione di semantica che il contratto non copriva — i tre
casi A/B/C non la nominano — e la segnalo perché è arbitrabile: l'alternativa (set-only, il primo rifiuto per
occupazione vince) è difendibile e produrrebbe `BlockedByUnit` anche quando l'ultimo tentativo era off-map.

Il controller la chiama in `HandleClickOnCell`, subito dopo il `Pop()` (`RTPlayerController.cpp:1636`). La
voce **non** si scrive lì: si registra l'intento, e la fase `Move` decide. È il vincolo 4 dei falsi punti di
partenza — ogni click esplorativo diventerebbe un fatto del replay.

**3. Il consumo, in un punto solo per tutti e tre i produttori** — `RTTurnManager.cpp`, ramo di collasso.

È la parte riprogettata dopo `F3`. Il ramo sta dentro `if (Path.Num() < 2)`, cioè la riga in cui player,
Scenario Harness e bot diventano lo stesso caso e da cui `FinalizeHexMovementOutcomes` li chiamerà tutti
`Stayed`. Due fonti, e nessuna copre l'altra:

| Produttore | Dove il piano viene rifiutato | Cosa si legge al collasso |
|---|---|---|
| Player · Harness | in pianificazione: il `Pop()` cancella la destinazione | il flag persistito e `RejectedMoveDestination` |
| Bot | mai in pianificazione — `FindPathForUnit` fallisce **qui** | `HasPlannedNormalMove()` + `PlannedCell`, ancora intatti |

```cpp
if (Unit->bMovePlanRejectedByOccupant) { /* player, harness */ }
else if (Unit->HasPlannedNormalMove()
    && URTHexSimLibrary::ClassifyWaypointCell(Snapshot, i, Unit->PlannedCell) == ERTHexWaypointReason::Occupied)
{ /* bot */ }
```

Il predicato «aveva dichiarato?» è `HasPlannedNormalMove()`, la sede unica che il WORK-ORDER indica per nome:
**non ne ho scritta una seconda**. Il motivo resta `ClassifyWaypointCell`. Il caso del bot non costa un file
in più, come previsto: `Bot/RTHexBotLibrary.cpp` non è stato toccato e resta fuori write-set.

**4. La riscrittura dell'esito** — dopo `BuildMoveLog`, accanto al ramo della topologia
(`RTTurnManager.cpp:7378`).

```cpp
if (bDeniedByOccupant[i] && Resolved[i].Outcome == ERTMoveOutcome::Stayed)
{
    MoveLog[i].Outcome = static_cast<uint8>(ERTMoveOutcome::BlockedByUnit);
    MoveLog[i].TgtCell = DeniedDestination[i];
}
```

⚠️ **La guardia `Stayed` non è cosmetica, ed è ciò che protegge i `BlockedContested` esistenti.** Chi si è
mosso, chi ha perso una cella contesa, chi è stato fermato dalla topologia o da un Overwatch ha un esito
avvenuto **dopo** la pianificazione, che è la spiegazione più vicina a ciò che il giocatore ha visto: la
stessa precedenza che il ramo della topologia applica chiedendo `Moved`. Le due condizioni sono disgiunte per
costruzione, e per questo il mio è un `if` indipendente e non un `else if` staccato dal proprio `if` da dodici
righe di commento.

**5. Il clearing** — quattro siti, nessuno dei quali duplica un altro.

| Sito | Sede | Caso |
|---|---|---|
| piano valido | `RTPlayerController.cpp:1656` | **C**: il replan valido cancella il rifiuto |
| piano ricostruito | `RTPlayerController.cpp:2083`, in `RebuildPlannedPath` | **C** via undo: copre `OnUndoWaypoint` **e** il passo indietro del puntatore, che passano entrambi di qui |
| unità spostata | `RTUnit.cpp`, in `PlaceOnCell` | il rifiuto è parte del piano e si consuma con lui, per ogni scrittore di `Cell` |
| fine fase `Move` | `RTTurnManager.cpp:7616` | consumo di turno, prima dei due rami del facing: vale per chi si è mosso, chi è rimasto fermo **e chi è morto** |

Il secondo sito sta in `RebuildPlannedPath` e non nei suoi due chiamanti, che il WORK-ORDER elencava
separatamente (`:2029`, `:2272`): entrambi passano di lì, e due copie della stessa riga divergerebbero alla
prima modifica.

## PUBLIC CONTRACT

Campi di `WAVE_DEV_LEAD.md` per la parte implementata. Non ridefiniscono `EXPECTED BEHAVIOR`: lo compilano.

| Campo | Valore |
|---|---|
| **Given** | un'unità viva in `Planning` il cui piano di movimento **finale** è negato perché la destinazione richiesta è occupata da un'altra unità — per rifiuto in pianificazione (player, harness) o per fallimento del percorso alla risoluzione (bot) |
| **When** | la fase `Move` si risolve e per quell'unità non esiste un percorso percorribile (`Path.Num() < 2`) |
| **Then** | la sua unica voce `Phase = Move, Category = Move` porta `Outcome = BlockedByUnit` **al posto** di `Stayed`, con `TgtCell` = destinazione richiesta |
| **Authority** | server / `ARTTurnManager`. La classificazione e la scrittura sono autoritative; il client non decide nulla |
| **Timing boundary** | nasce in `Planning` (o al collasso, per il bot), si consuma nella fase `Move`. Nessuna scrittura al lock-in: `RTTurnManager.cpp:3218` resta intatto |
| **Target/recipient** | l'unità che ha dichiarato. `SrcCell` = sua cella di partenza; la voce **non nomina l'occupante** |
| **Failure** | rifiuto per budget, cella non percorribile o fuori mappa ⇒ `Stayed`, invariato. Il motivo si legge, non si inventa |
| **Fallback** | assenza di rifiuto ⇒ `Stayed`, esattamente come prima. Additivo sul dato, sostitutivo sull'esito |
| **Ordering** | invariato: stessa voce, stessa posizione nell'ordine d'input. Nessuna voce aggiunta o rimossa |
| **SEED_SOURCE** | `none` — nessun RNG introdotto |
| **Editor-visible expectation** | in PIE, il turno 4 di `PIE-V01-COLL` legge «fermo: cella occupata» invece di «resta», col testo già esistente. `PREDICTED — NOT VERIFIED`: non ho aperto l'Editor e non posso osservarlo |

## FILES

Scritti — 4:

| File | Cosa |
|---|---|
| `Unit/RTUnit.h` | i due campi, `NoteMovePlanRejection`, `ClearMovePlanRejection`, forward declaration di `FRTHexSnapshot` |
| `Unit/RTUnit.cpp` | implementazione della funzione condivisa; clearing in `PlaceOnCell`; un include |
| `Player/RTPlayerController.cpp` | scrittura al rifiuto; clearing al piano valido e in `RebuildPlannedPath` |
| `Turn/RTTurnManager.cpp` | raccolta al collasso (tre produttori), riscrittura dell'esito, consumo di fine fase |

In `ASSIGNED_SCOPE` e **non** scritti — 6, con la ragione:

| File | Perché no |
|---|---|
| `Turn/RTTurnLogLibrary.cpp` | **no-op, come il punto 5 prevedeva e chiedeva di dichiarare.** Misurato: `RTTurnLogLibrary.cpp:362` traduce già `BlockedByUnit` → «fermo: cella occupata». ⚠️ Il WORK-ORDER cita `:402` per quel testo; a `:402` c'è un **commento** che nomina `BlockedByUnit`, la traduzione è a `:362`. Differenza di riferimento, non di sostanza |
| `Turn/RTHexSimLibrary.h` · `.cpp` | `ClassifyWaypointCell` esiste già ed è `public static`: riusarla non richiede di toccarla. Aggiungere un predicato «è occupata?» sarebbe il secondo vocabolario che il WORK-ORDER vieta |
| `Turn/RTHexSim.h` | `FRTPlannedMovement` non è stato esteso — vedi `IMPLEMENTED` punto 1: non sopravvive fra le fasi |
| `Player/RTPlayerController.h` | nessun membro o firma nuova: le tre chiamate sono a metodi di `ARTUnit` |
| `Turn/RTTurnManager.h` | i due array sono locali a `ResolveMovement` |

## TESTS

Owner **DEV-TEST**. Nessun file di test scritto da questa istanza.

⚠️ Osservato nel working tree condiviso, non da me: `RTPlayerInteractionTests.cpp`,
`RTHexMovementIntegrationTests.cpp` e `CollisionChoke.json` risultano modificati, e contengono già i casi A/B
e `HexMove.DenialAndStillnessAreDistinguishable`. Sono di `DEV-TEST:60316`, non li ho letti per derivarne
l'implementazione né toccati. Li nomino solo perché DEV-LEAD sappia che il write-set del working tree è di
**due** istanze.

## BUILD

`NOT RUN — dominio VALIDATION (ruolo DEV, motore non occupabile)`.

Nessuna build, nessun `rt-suite`, nessun Automation Test, nessuno Scenario Harness, nessun Editor, nessuna
sessione PIE. Unreal è rimasto libero per tutta la sessione.

Buildability statica curata per costruzione, e verificata per lettura:

- `URTHexSimLibrary::ClassifyWaypointCell` è `public static` (`RTHexSimLibrary.h:178`, `public:` a `:65`);
- `RTTurnManager.cpp` include già `Turn/RTHexSimLibrary.h` (`:32`); a `RTUnit.cpp` l'include è stato aggiunto;
- `FRTHexSnapshot` è una struct C++ **pura** (`RTHexSim.h:287`), non una `USTRUCT`: la forward declaration in
  `RTUnit.h` regge, e la firma che la usa non è una `UFUNCTION`, quindi UHT non la deve risolvere;
- `HasPlannedNormalMove()` è pubblica (`RTUnit.h:606`) e `const`;
- `FRTCellId` ha `FRTCellId() = default` (`RTCellId.h:41`), che rende validi `Init(FRTCellId(), N)` e il reset.

⚠️ `file modificato != build verificato` (§3). Nessuna di queste letture è una compilazione.

## TURNLOG / REPLAY

- **Nessun campo nuovo, nessun enumeratore nuovo, nessun bump di formato.** `BlockedByUnit` esiste da prima
  (`RTTurnLog.h:453`) e la sua dichiarazione — «fermata (o parziale) per cella occupata da un'unità ferma» —
  è letteralmente il caso. `Turn/RTTurnLog.h` non è stato toccato: resta `INTEGRATION-OWNED`.
- Cambiano il **valore** di `Outcome` e di `TgtCell` su una voce che esisteva già. Le tracce v2–v7 scritte in
  passato non cambiano significato e non richiedono migrazione.
- `Amount` resta quello di `BuildMoveLog`: `Entered.Num()`, cioè `0` per costruzione su chi non si è mosso.
  Non l'ho riassegnato a mano — il rischio 4 del WORK-ORDER è esattamente un `0` scritto da due posti.
- `ActionId` e `Priority` restano quelli letti dal catalogo da `BuildMoveLog`
  (`FindCoreAction("Action.Move")`): **nessun valore cablato** entra nel ramo nuovo.
- **Ordering invariato**: una voce per unità, nella posizione dell'ordine d'input. Il ramo riscrive, non
  inserisce.
- `bSlidThisMove[i]` legge l'esito **finale** dopo la riscrittura: con `BlockedByUnit` resta `false`, ed è
  corretto — un'unità ferma non scivola. L'invariante `Status.UnbalancedIffSlid` non è toccata.

🔴 **Corpus golden — rischio reale, non ipotetico.** `Tests/Golden/**` contiene `Movement.Collision` con
checksum. Se una sua unità dichiara un movimento poi negato per occupazione, il digest cambia. È il rischio 1
del WORK-ORDER, resta `INTEGRATION-OWNED`, e **non l'ho misurato**: misurarlo richiede di eseguire la suite.

## NETWORK / PRIVACY

Nessun degrado, misurato e non asserito.

- I due campi sono `UPROPERTY(Transient)`, **non** `Replicated`. Delta verificato fra `BASE_SHA` e il working
  tree con `git grep -c 'UPROPERTY(.*Replicated'`: **identico**, cinque file, nessuna occorrenza aggiunta.
- ⚠️ **L'acceptance criterion 6 non regge come è scritto, e non per colpa di questo write-set.** Prescrive che
  il conteggio «resta a `2` (le sole fixture di `RTServerOnlyGuardTests`)». Misurato su `BASE_SHA`, prima di
  qualsiasi mia riga: cinque file — `Core/RTServerOnlyGuard.{h,cpp}`, `Turn/RTIntentPrivacyLibrary.h` e due di
  test — per dieci occorrenze. Il criterio manderebbe rosso VALIDATION su una baseline che nessuno ha
  cambiato. Il **delta** è la lettura giusta, ed è `0`.
- Marcatura `RTServerOnly`: valutata, **non applicabile**. È una chiave di metadata su `USTRUCT`/`UCLASS`
  (`RTServerOnlyGuard.h:71`), non su singole proprietà; non ho introdotto alcun tipo nuovo, e marcare `ARTUnit`
  sarebbe un cambiamento di ampiezza estranea a #79. La difesa qui è che il dato non è replicato.
- ⚠️ Il dato **è** intento di pianificazione — la destinazione racconta cosa l'unità stava per fare — quindi
  non deve finire in un DTO diretto ai client. La guardia strutturale non lo vedrà mai, perché guarda i tipi
  marcati: se un giorno entrerà in `FRTIntentView`, sarà `FilterForTeam` a doverlo escludere.
- **Authority**: il flag viaggia sulla stessa strada di `PlannedWaypoints` e `PlannedPath` — scritto dal
  pianificatore, letto da `ARTTurnManager`, mai replicato — e non introduce un percorso di autorità nuovo.
  L'esito resta deciso dal server: `CLIENT PROPOSES → SERVER VALIDATES → SERVER APPLIES` è invariato.
- **Nessuna autorità gameplay in Blueprint**: i due campi non sono `BlueprintReadWrite` e le due funzioni non
  sono `UFUNCTION`. Deliberato — `PlannedWaypoints` accanto a loro è `BlueprintReadWrite`, e non ho seguito
  quella convenzione proprio per non aprire una porta di scrittura al piano da Blueprint.

## DETERMINISM

- Nessun RNG: `SEED_SOURCE: none`.
- Nessuna iterazione di `TMap`/`TSet`. I due array nuovi sono indicizzati per posizione, come
  `bStoppedByTopology` e `PlannedMoves`. L'unico accesso associativo è `Occupancy.Find(Cell)` **dentro**
  `ClassifyWaypointCell`, che è una lookup per chiave: non dipende dall'ordine di iterazione.
- L'esito non dipende da frame rate, timing di animazione o completamento asincrono: il ramo legge stato già
  fissato e scrive un `uint8`.
- La chiave resta `SrcCell`, la cella di partenza, «stabile perché `Cell` cambia dopo `PlaceOnCell`».

## SELF-REVIEW

| Punto | Esito |
|---|---|
| clearing su nuovo piano valido | ✅ `HandleClickOnCell` ramo di successo |
| clearing su undo / replan | ✅ `RebuildPlannedPath`, che copre entrambi i chiamanti |
| clearing su nuovo turno | ✅ consumo a fine fase `Move`, più `PlaceOnCell` per chi è stato spostato |
| death / cleanup | ✅ il clearing precede i due rami del facing, quindi vale anche per chi non è più `IsAlive()`; `IsValid` esclude solo chi è già distrutto, che non ha stato |
| un solo log entry per unità | ✅ il ramo **riscrive** la voce di `BuildMoveLog`, non ne aggiunge una: `Stayed` e `BlockedByUnit` restano alternativi, come arbitrato |
| `SrcCell` / `TgtCell` corrette | ✅ `SrcCell` invariata (partenza, da `BuildMoveLog`); `TgtCell` = destinazione richiesta |
| nessuna regressione dei `BlockedContested` | ✅ la guardia `Stayed` esclude ogni esito prodotto dal resolver |
| lifetime / GC | ✅ nessun puntatore trattenuto: due valori scalari sul `AActor` che li possiede |
| serialization | ✅ `Transient` non entra nel salvataggio; nessun campo serializzato aggiunto |
| replay | ✅ nessuna migrazione; cambia il valore di una voce esistente |
| stale pending fra turni | ✅ quattro siti di clearing, elencati sopra |
| bot / scenario path | ⚠️ bot **coperto** al collasso; harness **non coperto** — è `INTEGRATION REQUIRED`, per assegnazione |
| ordine deterministico | ✅ vedi `## DETERMINISM` |

Un buco che dichiaro invece di lasciarlo trovare: se `ResolveMovement` esce dal `return` iniziale
(`if (!World)`), il clearing di fine fase non gira. È prima di ogni lettura e di ogni scrittura del log, e
senza `World` non c'è partita in cui il residuo possa essere osservato — ma è l'unica via in cui lo stato
sopravvive a un turno.

## INTEGRATION REQUIRED

Uno solo, previsto dal WORK-ORDER («il lavoro qui è serializzato: DEV-LEAD lo applica dopo il contributo di
DEV-MAIN, riusando la funzione che DEV-MAIN espone»). **Non applicato**: `ScenarioHarness/**` è
`INTEGRATION-OWNED` e non l'ho toccato.

🔴 **Senza questa patch l'acceptance criterion 4 non è soddisfatto e `CollisionChoke` resta a `0`.** Il
percorso dell'harness continuerebbe a produrre `Stayed`: sarebbe il difetto di oggi con due produttori su tre
corretti. La funzione da chiamare esiste già ed è pubblica; non serve altro codice mio.

Diff proposto — `Source/RefactorTactics/ScenarioHarness/RTScenarioSession.cpp`, ramo di
`BuildCompositeHexPath` (`:1339-1350`):

```diff
 		const FRTHexPathResult Path = URTHexSimLibrary::BuildCompositeHexPath(Snapshot, UnitId, Intent.Move);
 		if (Path.Path.Num() >= 2)
 		{
 			Unit->PlannedWaypoints = Intent.Move;
 			Unit->PlannedPath = Path.Path;
 			Unit->PlannedCell = Path.Path.Last();
+			// #79, caso C: un piano valido cancella un rifiuto precedente, come nel controller.
+			Unit->ClearMovePlanRejection();
 		}
 		else
 		{
+			// #79: la STESSA funzione del controller, non una copia. La destinazione richiesta e' l'ultimo
+			// waypoint dichiarato dallo scenario; senza waypoint non c'e' destinazione da registrare.
+			if (Intent.Move.Num() > 0)
+			{
+				Unit->NoteMovePlanRejection(Snapshot, UnitId, Intent.Move.Last());
+			}
 			Notes.Add(FString::Printf(TEXT("turno %d: percorso rifiutato per '%s': l'unita' resta ferma"),
 				TurnIndex + 1, *Intent.UnitId));
```

⚠️ Due note per chi lo applica. (1) `Snapshot` e `UnitId` sono già in scope a `:1332-1334`, non serve
ricalcolarli. (2) La riga `UE_LOG(Warning, "[RT-Test] … percorso rifiutato …")` sotto **può restare**: è
diagnostica del runner e non è più l'unica traccia: la sua sopravvivenza non riapre il difetto, la sua
rimozione non lo chiude.

## VALIDATION REQUESTED

Gate che occupano Unreal, per DEV-LEAD. Nessuno eseguito da me, nessuno eseguibile da questo ruolo.

| Gate | Perché in scope |
|---|---|
| compile | il write-set tocca quattro file C++, uno dei quali header incluso ovunque |
| Automation `RefactorTactics.HexMove.*`, `PlayerInput.*`, `UI.*` | il produttore delle voci `Move` è cambiato |
| Scenario `Movement/CollisionChoke` | è il caso diagnostico, e **misura l'harness**: verde solo con la patch di `INTEGRATION REQUIRED` |
| `TURNLOG/REPLAY` · `DETERMINISM` | §8 punto 3: a valle del produttore toccato, anche senza file loro modificati |
| corpus golden `Movement.Collision` | il digest può cambiare. **Misurare prima di rigenerare** |
| `PIE-V01-COLL` (c) · `PIE-V01-LOG` | oracolo umano, `USER_REQUIRED` |

## RISKS

1. **La semantica «l'ultimo tentativo governa» è mia, non del contratto.** I casi A/B/C non coprono
   «occupato, poi rifiutato per altro motivo». Ho scelto che l'ultimo rifiuto governi. Arbitrabile: dirlo qui
   costa una riga, scoprirlo da un test rosso costa una sessione.
2. **Corpus golden.** Vedi `## TURNLOG / REPLAY`. Non misurato, e la misura non è di questo ruolo.
3. **Il caso del bot è entrato tardi.** L'ho implementato dopo che `bcde0be9` ha cambiato il WORK-ORDER; è la
   parte con meno tempo di riflessione addosso, ed è quella che nessun test del working tree sembra coprire
   oggi — DEV-TEST punto 6 la chiede in `RTHexMovementIntegrationTests.cpp`.
4. **Due istanze DEV scrivono lo stesso working tree.** Il mio write-set e quello di DEV-TEST sono disgiunti
   per path, ma un `git add -A` di chiunque catturerebbe entrambi a metà. Il WORK-ORDER lo vieta già
   esplicitamente; lo ripeto perché è la via più breve per un `PRODUCED_SHA` che non descrive ciò che dichiara.
5. **`HasPlannedNormalMove()` è ora anche il predicato di una voce di TurnLog.** Era un predicato di
   pianificazione; chi lo cambierà in futuro cambierà anche quali unità dichiarano un diniego. Il commento nel
   ramo lo dice, ma la dipendenza non è visibile dalla sua definizione.

## NOT RUN

| Elemento | Motivo |
|---|---|
| build / compile | dominio VALIDATION; il ruolo DEV non occupa Unreal |
| `rt-suite`, Automation, Scenario Harness | come sopra |
| PIE, `PIE-V01-COLL`, `PIE-V01-LOG` | dominio EDITOR / VALIDATION |
| digest del corpus golden | richiede l'esecuzione della suite |
| verifica della patch harness | non applicata: `INTEGRATION-OWNED` |

§6: nessuna di queste voci si legge `PASS`. Nessun campo di questo contributo è un verdetto di §7.

STATUS:   READY
REASON:   n/a
UNBLOCK:  n/a

`READY` sul mio `ASSIGNED_SCOPE`, che è completo: i punti 1-4 e 6 sono implementati, il punto 5 è il no-op
dichiarato. **Non** è `READY` sull'acceptance criterion 4, che richiede tre produttori su tre: il terzo
dipende dalla patch di `## INTEGRATION REQUIRED`, che appartiene a DEV-LEAD per assegnazione, non per
impedimento. Non ho committato: il consolidamento e il `PRODUCED_SHA` sono di DEV-LEAD.
