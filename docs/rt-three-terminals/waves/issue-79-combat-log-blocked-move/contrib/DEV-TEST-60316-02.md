=== RT3 CONTRIB ===

FROM:            DEV-TEST:60316
TO:              DEV-LEAD
FEATURE:         issue-79-combat-log-blocked-move
WAVE_ID:         issue-79-combat-log-blocked-move/1
CREATED:         2026-09-06 16:02
SUPERSEDES:      contrib/DEV-TEST-60316-01.md
BASE_SHA:        586ad5942b286fcb655c0080c6578cc592390461
PRODUCED_SHA:    586ad5942b286fcb655c0080c6578cc592390461   (= BASE_SHA: nessun commit prodotto)
WORKTREE:        uncommitted
ASSIGNED_SCOPE:  WORK-ORDER.md § DEV-TEST — ASSIGNED_SCOPE
CONTRACT_SOURCE: WORK-ORDER.md § CONTRACT SOURCE · § EXPECTED BEHAVIOR · § Arbitrati DEV-LEAD

## RT3 INIT

```text
Tipo:             DEV-TEST
Instance:         DEV-TEST:60316
Feature:          issue-79-combat-log-blocked-move
Wave:             issue-79-combat-log-blocked-move/1
Branch:           fix/79-blocked-move-turnlog
Base SHA:         586ad594
HEAD:             586ad594 all'avvio → 02a48b58 alla chiusura (vedi § Preflight)
Working tree:     modifiche di altre istanze presenti, non toccate
Assigned scope:   3 path, tutti scritti
Contract source:  WORK-ORDER.md
```

## Preflight §4 e precondizioni §5 — misurate

`§4`: quattro campi su quattro risolti, nessun placeholder. L'ingresso è un file esistente e leggibile.
Il `BLOCKED / MISSING_INPUT` di `-01` è chiuso da **APPLIED BY LEAD**.

`§5`, all'avvio:

```text
git rev-parse --abbrev-ref HEAD  -> fix/79-blocked-move-turnlog     = BRANCH dichiarato
git rev-parse HEAD               -> 586ad5942b286f...                = BASE_SHA dichiarato
git status --porcelain           -> ?? RT3-VALIDATION-a59671c.md     (untracked, altra istanza)
```

### ⚠️ Due scostamenti, dichiarati invece che taciuti

**1. `BASE_SHA` di ingresso ≠ `BASE_SHA` della busta del WORK-ORDER.** Il work order dichiara
`a59671c8`; il mio ingresso dichiara `586ad594`. Non è un conflitto: i tre commit che li separano
sono **solo documenti di questa wave**, misurato —

```text
git diff --name-only a59671c8 586ad594 -- Source/ Scenarios/ Tests/   ->  (vuoto)
```

— quindi ogni misura di codice del work order resta valida su `586ad594`.

**2. 🔴 `HEAD` si è mosso durante l'authoring:** `586ad594` → `02a48b58`. Misurato il contenuto prima di
proseguire: il commit è `docs(rt3): il referto VALIDATION della wave #79 e' BLOCKED…` e tocca **un solo
file**, `RT3-VALIDATION-a59671c.md`. Nessun file di `Source/`, `Scenarios/` o `Tests/`.

§5 dichiara `NON VALIDA` una misura la cui finestra vede `HEAD` muoversi. **Qui non si applica, e la
ragione è di sostanza, non di comodo**: questo contributo non contiene misure — è authoring statico,
ogni gruppo è `NOT RUN`, e nessuna riga qui è un verdetto. Ciò che lo spostamento di `HEAD` poteva
invalidare è la base su cui ho costruito le *fixture*, e quella base non è cambiata.

**Working tree condiviso.** Alla chiusura il tree contiene anche lavoro di altre istanze —
`Unit/RTUnit.{h,cpp}`, `Player/RTPlayerController.cpp`, `Turn/RTTurnManager.cpp` (DEV-MAIN),
`WORK-ORDER.md` riscritto (DEV-LEAD), `RT3-EDITOR-a59671c.md` (EDITOR). §5 lo prevede
esplicitamente. **Non ho toccato nessuno di quei file**, e nessuno di essi entra nel mio `WRITE_SET`.

### Il WORK-ORDER è cambiato mentre lavoravo, e mi riguardava

Alle 15:49 DEV-LEAD ha riscritto l'ingresso rispondendo ai Finding di VALIDATION. Due modifiche cadono
nel mio scope e sono state **recepite**, non ignorate:

- **DEV-TEST punto 6** (nuovo, da Finding `…/1-F3`): serve «almeno un caso sul percorso **non-harness**»,
  e il work order nomina il caso **bot** come complemento naturale in `RTHexMovementIntegrationTests.cpp`.
  → coperto da `HexMove.DeclaredDestinationDeniedByOccupantDeclaresIt`.
- **Criterio di accettazione 4 riscritto**: non più «due chiamanti» ma **tre** — player, Scenario
  Harness e bot — che devono produrre lo stesso esito per lo stesso stato.
  → è l'oracolo del test bot, ed è la ragione del secondo `EXPECTED FAILURE`.

## WRITE_SET

```text
Source/RefactorTactics/Tests/RTPlayerInteractionTests.cpp        + 4 test, + 3 helper
Source/RefactorTactics/Tests/RTHexMovementIntegrationTests.cpp   + 2 test, + 1 helper, + 1 include
Scenarios/Movement/CollisionChoke.json                            1 expected, 2 note
docs/…/contrib/DEV-TEST-60316-02.md                               questo file
```

Tutti dentro l'`ASSIGNED_SCOPE`. Nessun file di produzione toccato. Nessun `.uasset`/`.umap`.
Fine riga **CRLF** preservato su tutti e tre i file (verificato: CR = LF, nessun raddoppio).

## AUTOMATION

Sei test, per nome. Gli oracoli vengono da § EXPECTED BEHAVIOR e dagli § Arbitrati DEV-LEAD — in
particolare l'arbitrato 1, *«la voce di blocco sostituisce `Stayed`, non si aggiunge»*, che è la
ragione per cui **ogni** test asserisce la **cardinalità** delle voci e non solo il loro contenuto.

| # | Test | Caso | Cosa asserisce |
|---:|---|---|---|
| 1 | `RefactorTactics.PlayerInteraction.DeniedMoveDeclaresTheDenial` | **B** | una sola voce `Move`; `Outcome = BlockedByUnit`; `Phase = Move`; `SrcCell` = partenza; `TgtCell` = **destinazione richiesta**; `ActionId = Action.Move`; `Priority` dal catalogo; `Amount = 0`; l'unità non si è mossa |
| 2 | `RefactorTactics.PlayerInteraction.UndeclaredMoveDoesNotDeclareADenial` | **A** | una sola voce; `Outcome = Stayed`; **zero** `BlockedByUnit` su tutto il log |
| 3 | `RefactorTactics.PlayerInteraction.ReplanAfterADenialWins` | **C** | **zero** `BlockedByUnit`; una sola voce; `Outcome = Moved`; `TgtCell` = destinazione **corretta** |
| 4 | `RefactorTactics.PlayerInteraction.BudgetDenialIsNotAUnitDenial` | confine | un rifiuto per **budget** resta `Stayed`; zero `BlockedByUnit` |
| 5 | `RefactorTactics.HexMove.DenialAndStillnessAreDistinguishable` | acc. 2 | i due esiti **non coincidono**, misurati nello **stesso turno**; + due esecuzioni identiche → stessa sequenza canonica |
| 6 | `RefactorTactics.HexMove.DeclaredDestinationDeniedByOccupantDeclaresIt` | acc. 4 | il **terzo produttore** (sola `PlannedCell`, nessun `PlannedPath`) produce la stessa voce del player |

### Perché i casi A/B/C passano dal controller e non dai campi `Planned*`

È la scelta di progetto più importante di questo contributo. `Actions.Move.PathBlocked` produce **già**
`BlockedByUnit` oggi, e un test che scrivesse `PlannedPath` a mano sarebbe verde **prima** di qualunque
correzione — misurerebbe il troncamento del resolver, che funziona, lasciando intatto sotto un verde il
difetto della #79. La differenza è quella che il work order misura agli stadi 4-5: qui il percorso non
arriva mai al resolver, perché `BuildCompositeHexPath` lo rifiuta in pianificazione e
`PlannedWaypoints.Pop()` non lascia niente dietro di sé.

Ogni test del caso B porta perciò una **premessa esplicita** — `PlannedWaypoints.Num() == 0` e
`PlannedPath.Num() == 0` — che fallisce con un messaggio proprio se la fixture scivolasse sull'altra
strada, invece di lasciare che il test passi per la ragione sbagliata.

### Anti-vacuità — le mutazioni che devono far cadere ciascun test

Richiesto dal punto 5 dell'`ASSIGNED_SCOPE`. `Stayed` e `BlockedByUnit` descrivono **entrambi**
un'unità che non si è mossa: la posizione finale non distingue il fix dal difetto, e senza queste
mutazioni il caso B non prova nulla.

| Mutazione sul codice di produzione | Deve cadere |
|---|---|
| non scrivere affatto la voce di diniego (= stato di `main` oggi) | 1, 5, 6 e `CollisionChoke` |
| scrivere `BlockedByUnit` **incondizionatamente** | 2 e 4 |
| **aggiungere** la voce invece di sostituire `Stayed` | 1, 2, 3, 6 (tutte asseriscono `Voci.Num() == 1`) |
| `TgtCell = SrcCell` (la cella di partenza) | 1, 6 |
| scrivere la voce al **click** invece che al piano finale | 3 |
| non azzerare il rifiuto al replan valido | 3 |
| `Priority` cablata a `0` invece che dal catalogo | 1, 6 |
| `Amount` diverso da `0` | 1, 6 |
| classificare **ogni** rifiuto come occupazione | 4 |
| coprire il solo player e non il terzo produttore | 6 |
| ordinare le voci per iterazione di `TMap`/`TSet` | 5 |

⚠️ Le mutazioni sono **dichiarate, non eseguite**: eseguirle richiede build e suite, che sono di
VALIDATION. Il punto 7 dei criteri di accettazione chiede che siano verificate sul file *prima* del fix
— è una misura, e appartiene a chi misura.

## SCENARIOS

`Scenarios/Movement/CollisionChoke.json` — JSON rivalidato dopo la modifica (parser: valido).

| Assertion | Prima | Dopo | Perché |
|---|---:|---:|---|
| `Move/BlockedContested` | 4 | **4** | invariato — in scope come **regressione** (§8 punto 3) |
| `Move/BlockedByUnit` | 0 | **1** | il solo valore che questa wave cambia |
| `Move/Moved` | 1 | **1** | invariato — regressione |
| `TurnsCompleted` | 4 | **4** | invariato — non dipende dagli esiti |
| `UnitAtCell` ×2 | — | — | invariati |

**Il `1` è derivato, non copiato.** La regola è «una voce per **tentativo negato nel piano finale**», e
in questo scenario il tentativo negato è **uno solo**: il T4. I T1-T2 sono contesa simultanea
(`BlockedContested`) e al T3 `A1` non dichiara niente (`Stayed`) — nessuno dei tre è un diniego da
unità ferma. Convergono su questa lettura il § *CONTRACT SOURCE* del work order e il § 5.1 di
`RT3-VALIDATION-a59671c.md`, arrivati alla stessa regola per strade separate.

**Le note.** La nota che documentava lo `0` come gap noto è stata **riscritta**, non cancellata: la sua
storia — prima stesura `1` derivata dalla regola, misura del 2026-09-04 che diede `0`, file riscritto
per pinnare il buco — è ciò che impedisce al prossimo lettore di leggere il `1` come un
riallineamento. Aggiunta `_nota_stato_dopo_79`, che dichiara **perché la riga sarà rossa** e cosa serve
per farla passare *davvero* (criterio di accettazione 8).

⛔ L'expected **non** è stato allargato a budget esaurito, cella non percorribile e fuori mappa: il
contratto li lascia `Stayed`, ed è il confine che il test 4 difende.

## VALIDATORS

Nessuno aggiunto. Il vocabolario dei motivi esiste già (`ERTHexWaypointReason`, `ClassifyWaypointCell`)
e il contratto prescrive di delegargli la classificazione invece di scriverne un secondo. Un validator
nuovo qui sarebbe il secondo vocabolario che il work order vieta.

## DIMENSIONS COVERED

Derivate dal `WRITE_SET` con §8, incluso il punto 3.

| Dimensione | Stato | Dove / motivo |
|---|---|---|
| SUCCESS | coperta | test 1 (voce corretta), 3 (movimento eseguito), 6 (terzo produttore) |
| INVALID | coperta | test 4: rifiuto per budget → `Stayed` con l'esito che gli compete. Il «reason code» del contratto è l'`Outcome` stesso |
| BOUNDARY | coperta | test 4 (confine fra i motivi di rifiuto); test 2 (zero dichiarazioni); `Amount = 0` in 1 e 6 |
| FALLBACK | coperta | test 2 e 4: assenza di stato di rifiuto ⇒ `Stayed`, il fallback deterministico che § EXPECTED BEHAVIOR dichiara |
| DETERMINISM | coperta | test 5: due esecuzioni identiche, stessa sequenza canonica. `SEED_SOURCE: none` — nessun RNG sul percorso |
| ORDERING | coperta | test 5: firma canonica per voce, confronto posizionale. Un ordine da iterazione di `TMap`/`TSet` cade qui |
| SERIALIZATION | coperta **indirettamente** | il contratto dichiara *nessun campo nuovo, nessun enumeratore nuovo, nessun bump*: cambiano il **valore** di `Outcome` e `TgtCell` su una voce che esisteva già. I test asseriscono i campi serializzati (non `DescribeEntry`), quindi un campo che non sopravvive al formato cade. ⚠️ Il roundtrip vero e proprio è `Simulation.GoldenCorpus*` — vedi § COMMANDS |
| TURNLOG | coperta | è il centro: category, outcome, src, tgt, unit id, action id, priority, amount, ordering, **cardinalità** |
| REPLAY | **parziale** | i test coprono la voce prodotta; il corpus `*.rttl` è `Tests/Golden/**`, fuori scope — vedi § INTEGRATION REQUIRED e § GOLDEN |
| NETWORK | `N/A` | `REASON`: il write-set è di soli test e dati di scenario; nessuna `UPROPERTY(Replicated)` è raggiungibile da qui. Il non-degrado è il criterio 6 del work order e si misura con un `grep`, non con un test mio |
| PRIVACY | `N/A` | `REASON`: come sopra — e §7 pone comunque il tetto `PASS` a `VALIDATION`. ⚠️ **Non è un'assenza di rischio**: il § 5.5 di VALIDATION segnala che il diniego rivela l'**occupazione di una cella avversaria**. Se DEV-MAIN colloca lo stato in modo replicabile, serve un canary, e il suo formato è una decisione documentale che chiedo a DEV-LEAD invece di fissarla qui |

## COMMANDS

Comandi proposti, **non eseguiti**. Il ruolo DEV non occupa Unreal.

### 🔴 Prima di tutto: compilare. `rt-suite.ps1` non compila.

```powershell
& "D:\EpicGames\UE_5.8\Engine\Build\BatchFiles\Build.bat" RefactorTacticsEditor Win64 Development `
    -project="D:\Repositories\refactor-tactics-main\RefactorTactics.uproject" -waitmutex
```

Dimostra: che i sei test **esistono nel binario**. Cercare `Result: Succeeded`.
⚠️ Senza questo passo la suite misura il binario precedente e un `PASS` non lega il verdetto al
sorgente: i test nuovi semplicemente non ci sarebbero, e `found = 0` si legge come «tutto verde».

### Poi, in quest'ordine

| # | Comando | Cosa dimostra |
|---:|---|---|
| 1 | `./scripts/rt-suite.ps1 -Filter RefactorTactics.PlayerInteraction` | casi **A**, **B**, **C** e il confine (test 1-4) |
| 2 | `./scripts/rt-suite.ps1 -Filter RefactorTactics.HexMove` | distinguibilità, ordering/determinismo, terzo produttore (test 5-6) **e** la non-regressione di `ContestedCellStopsBoth`, `Move.PathBlocked`, `Move.CellConflict`, che vivono sotto lo stesso filtro |
| 3 | `./scripts/rt-suite.ps1 -Filter RefactorTactics.Scenario` | `CollisionChoke` via `Scenario.EveryShippedScenarioRuns` — il percorso **harness** |
| 4 | `./scripts/rt-suite.ps1 -Filter RefactorTactics.Simulation.GoldenCorpus` | se e quali corpus `*.rttl` divergono. `GoldenCorpusMatches` è il gate; `GoldenDivergenceNamesTheField` dice **quale campo** |
| 5 | `./scripts/rt-suite.ps1` | la suite intera, per il verdetto di wave |

`-WaitMinutes <n>` se il motore è occupato da un'altra sessione.

⚠️ **Il comando 2 non è solo per i test nuovi.** Il work order mette `MOVEMENT` in scope come
regressione (§8 punto 3): se la riscrittura dell'outcome disturbasse il percorso già funzionante, i tre
test citati sono i primi a cadere, e stanno sotto lo stesso filtro senza costare una run in più.

⚠️ **L'ordine 3 → 4 conta.** Un `CollisionChoke` verde con un corpus divergente è la firma del difetto
che il criterio 4 vieta: l'evento emesso dentro l'harness invece che nel punto di collasso comune.

## EXPECTED FAILURES

Un test derivato dal contratto può essere corretto e rosso insieme. Dichiararlo impedisce che a valle
venga letto come regressione — e impedisce a me di ammorbidirlo.

**1. Tutti e sei i test, e `CollisionChoke`, finché il lavoro di DEV-MAIN non è integrato.**
Su `main` la voce di diniego non esiste: i test 1, 5, 6 e l'assertion dello scenario sono rossi **per
costruzione**. È lo stato atteso di un test scritto prima del fix, non un difetto del test.
I test 2, 3 e 4 sono invece **verdi già oggi** (asseriscono l'assenza del diniego): sono i gemelli
negativi, e il loro verde su `main` è ciò che li rende utili — se cadessero dopo il fix, il fix
sarebbe indiscriminato.

**2. 🔴 `CollisionChoke.json` resta rosso anche a fix del player completato.**
Lo scenario gira via `RTScenarioSession`, che è `INTEGRATION-OWNED` e che il work order fa applicare a
DEV-LEAD **dopo** il contributo di DEV-MAIN. Finché quel sito non registra il rifiuto, l'assertion vale
`0` contro un contratto che dice `1`. ⛔ Non è un motivo per riportare l'expected a `0`: è il
serializzarsi del lavoro, dichiarato dal work order stesso.

**3. 🔴 `HexMove.DeclaredDestinationDeniedByOccupantDeclaresIt` — il caso che segnalo a DEV-LEAD.**
L'oracolo è il criterio di accettazione 4: *tutti e tre* i produttori, stesso esito. Il bot non passa
da `HandleClickOnCell`, quindi non attraversa il ramo che registra il rifiuto: se lo stato nuovo è
scritto **solo** dal controller, questo test è rosso mentre il caso B passa — ed è esattamente la
condizione che il work order descrive come *«il difetto chiuso per un produttore su tre»*.

Il work order indica già la strada e non costa un file in più: la destinazione del bot è ancora
leggibile al collasso (`PlannedCell`), e il predicato «ha dichiarato» ha già una sede unica,
`ARTUnit::HasPlannedNormalMove()`. Il test asserisce quella premessa esplicitamente, così che se cade
si veda **dove**.

⚠️ Dichiaro l'oracolo, non l'esito: l'implementazione è nel working tree di un'altra istanza, non
committata e in evoluzione. Se al consolidamento il caso bot risulta coperto, questo `EXPECTED FAILURE`
decade — e il test resta come regressione.

## INTEGRATION REQUIRED

Tre richieste. Nessuno di questi file è stato toccato.

### 1. `Source/RefactorTactics/ScenarioHarness/RTScenarioSession.cpp` — `INTEGRATION-OWNED`

- **Test richiesto**: nessun test nuovo. È `CollisionChoke.json`, già scritto, che resta rosso.
- **Motivo**: è il **secondo produttore**. Lo scenario è l'unico caso diagnostico della wave e gira
  solo di qui; il file è assegnato a DEV-LEAD proprio perché i due scope si toccano.
- **Patch concettuale**: al ramo di rifiuto — `:1348-1350` sul sorgente misurato da VALIDATION (⚠️ il
  `:1338` che circola nei commenti della issue è un puntatore stantio) — chiamare la **stessa**
  funzione che DEV-MAIN espone per il controller, invece di limitarsi a `Notes` + `UE_LOG`. Due copie
  della regola divergerebbero alla prima modifica, e la divergenza sarebbe visibile solo in PIE.

### 2. Il terzo produttore — file di DEV-MAIN (`Turn/RTTurnManager.cpp`, `Unit/RTUnit.*`)

- **Test richiesto**: `RefactorTactics.HexMove.DeclaredDestinationDeniedByOccupantDeclaresIt`, già scritto.
- **Motivo**: criterio di accettazione 4, riscritto dopo il Finding `…/1-F3`. Il bot dichiara solo
  `PlannedCell` e non ha alcun ramo di rifiuto in pianificazione.
- **Patch concettuale**: nel punto di collasso comune, riconoscere anche il caso in cui l'unità **aveva
  dichiarato** (`HasPlannedNormalMove()`) e il percorso è risultato vuoto perché la destinazione è
  occupata da un'unità ferma — senza scrivere un secondo predicato di «ha dichiarato», che è
  precisamente ciò che il work order vieta al punto 6.

### 3. `Source/RefactorTactics/Turn/RTTurnLog.h` — `INTEGRATION-OWNED`

- **Test richiesto**: nessuno oggi.
- **Motivo**: **si chiede solo se serve**. Il contratto dice che `BlockedByUnit` basta e che non c'è
  bump. Se durante l'implementazione emergesse la necessità **dimostrata** di un valore proprio — per
  distinguere «negato prima di partire» da «fermato per strada» — è una richiesta bloccante verso
  DEV-LEAD, e i miei test cambierebbero di conseguenza: il test 1 asserisce `BlockedByUnit`, e un
  enumeratore nuovo lo renderebbe l'oracolo sbagliato.

## GOLDEN

`Tests/Golden/**` è `INTEGRATION-OWNED`. **Non ho rigenerato niente e non lo propongo.**

Il work order segnala `Movement.Collision` come rischio potenziale e chiede che la validazione misuri
il digest reale prima di agire. Ho letto lo scenario sorgente, e riporto il reperto perché restringe
l'area da misurare:

```text
Scenarios/Movement/Collision.json:  units A1(-1,0,0) B1(1,0,0)
                                    turns: UNO — A1 e B1 puntano entrambe (0,0,0)
```

**Un solo turno, e le due unità contendono la stessa cella.** Nessuna dichiara verso una cella
occupata da un'unità **ferma**: al momento del planning (0,0,0) è libera, quindi il percorso è
accettato e il fermo avviene nel resolver, come `BlockedContested`. Il caso della #79 — rifiuto **in
pianificazione** — non si presenta.

⇒ **Ipotesi: il digest di `Movement.Collision` non cambia.** È un'ipotesi derivata dalla lettura dello
scenario, **non una misura**: la conferma è `RefactorTactics.Simulation.GoldenCorpusMatches`, comando 4.
Gli altri sette corpus non contengono movimento contestato e sono a maggior ragione improbabili — ma
il gate li copre tutti in una run, quindi non c'è ragione di restringerlo.

⚠️ Se un corpus **divergesse**, `GoldenDivergenceNamesTheField` dice quale campo: una divergenza su
`Outcome`/`TgtCell` di una voce `Move` sarebbe attesa e da motivare; su qualunque altro campo sarebbe
una **scoperta**, e la rigenerazione andrebbe fermata.

## RISKS

1. **Il rischio maggiore è il verde sbagliato, non il rosso.** Un `CollisionChoke` che passa perché
   l'evento è emesso dentro `RTScenarioSession` soddisfa lo scenario e lascia la partita muta — è il
   difetto di oggi spostato di un livello. I test 1-4 e 6 esistono per questo: nessuno di loro passa
   dall'harness.
2. **`HandleClickOnCell` è un sito di produzione che tre istanze stanno guardando insieme.** Le mie
   fixture lo pilotano e le sue premesse (`PlannedWaypoints.Num() == 0` dopo il rifiuto) dipendono dal
   `Pop()`. Se DEV-MAIN cambiasse quel ramo per **conservare** il waypoint, le premesse cadrebbero con
   un messaggio proprio — cadono in modo leggibile, non silenzioso — ma andrebbero riviste.
3. **Il test 5 confronta due esecuzioni e include `UnitId` nella firma canonica.** Se cadesse sul solo
   `u<id>`, la causa sarebbe l'assegnazione degli `StableUnitId` fra due mondi e non la voce. È
   comunque una scoperta — quegli id entrano nel formato serializzato — ma va letta per quello che è.
4. **`Amount = 0` è un valore, non un'assenza**, e i test 1 e 6 lo asseriscono. Se DEV-MAIN lo
   riassegnasse a mano invece di lasciare quello che `BuildMoveLog` scrive, ci sarebbero due sorgenti
   dello stesso zero da tenere d'accordo. Il work order lo elenca come rischio 4.
5. **Il caso bot riproduce la *forma* del piano del bot, non il bot.** Nessuna euristica di
   `URTHexBotLibrary` gira nel test: far girare il bot metterebbe la sua scelta di destinazione fra la
   premessa e l'asserzione, e il test misurerebbe due cose insieme. È dichiarato nel commento del test.
6. **PIE resta scoperto e non è colmabile da qui.** `PIE-V01-LOG` e la clausola (c) di `PIE-V01-COLL`
   chiedono un oracolo umano: che la riga del combat log del turno 4 legga «fermo: cella occupata»
   invece di «resta». Nessun test headless lo prova.

## NOT RUN

| Gruppo | Stato | Motivo |
|---|---|---|
| Build / compile | `NOT RUN` | authoring — esecuzione a VALIDATION |
| Automation (test 1-6) | `NOT RUN` | authoring — esecuzione a VALIDATION |
| Scenario Harness (`CollisionChoke`) | `NOT RUN` | authoring — esecuzione a VALIDATION |
| Golden corpus | `NOT RUN` | authoring — esecuzione a VALIDATION |
| Mutazioni anti-vacuità | `NOT RUN` | richiedono build e suite: sono misure |
| PIE | `NOT RUN` | dominio EDITOR / VALIDATION |

```text
performed = 0 != PASS
```

Nessuna riga di questo contributo è un verdetto. Non ho eseguito build, `rt-suite`, Automation,
Scenario Harness né PIE, e non ho aperto Unreal.

---

STATUS:   READY
REASON:   n/a
UNBLOCK:  n/a

Il contributo è completo sul proprio scope: sei test, uno scenario, tutte le dimensioni dichiarate.
I due `EXPECTED FAILURE` strutturali (§ 2 e § 3) **non** sono blocchi di questo ruolo — sono lavoro di
altri scope, già assegnato dal work order, e sono riportati in § INTEGRATION REQUIRED con path, test,
motivo e patch concettuale come impone il mandato.

⚠️ Una sola domanda aperta, non bloccante e indirizzata a DEV-LEAD: se lo stato di rifiuto finisse per
toccare l'intento in modo osservabile da un client non autorizzato, `PRIVACY` smetterebbe di essere
`N/A` e servirebbe un canary. Il formato del marcatore è una decisione documentale, e il repository ha
già precedenti con semantica precisa (`RTHexBotIntegrationTests.cpp`, `RTScenarioRunner.cpp`): la
chiedo invece di fissarla in un test.
