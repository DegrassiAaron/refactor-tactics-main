# Execution graph — la topologia del lavoro, e nient'altro
#
# Owner della sola cosa che oggi non ha un owner strutturato: **chi blocca chi, e chi fornisce
# cosa**. Tutto il resto — stato delle feature, delle issue, dei checkpoint, delle sedute, degli
# scenari — resta dei rispettivi owner, e questo file NON lo ripete.
#
# ⛔ **Questo file non ha piu' una vista.** Era il tab «Execution Map» del Control Center, alimentato da
# `project-graph.json`: entrambi sono usciti il 2026-08-21 (D-181), col Feature Registry che li
# generava. Nessuno script legge questo file. Resta perche' la topologia scritta qui — chi blocca chi —
# non e' ricavabile da nessun'altra parte, non perche' qualcosa la consumi.
#
# ## Cosa NON va scritto qui
#
#   status / state / open / closed     lo stato non e' topologia. Il generatore lo deriva dagli
#                                      owner; dove non ha una fonte offline scrive `UNKNOWN`,
#                                      che e' un'informazione — «inventato READY» non lo e'.
#   il titolo di una issue             vive su GitHub e cambia senza avvisare
#   `blocks:`                          e' l'inversa di `requires`, e il generatore la deriva.
#                                      Scrivere entrambe crea due verita' che divergono al primo
#                                      edit di una sola.
#   `junction: true`                   deriva dal numero di ingressi hard
#   `domain_group:` derivabile         lo deriva il generatore: `feature.issues` del registry dice
#                                      quali feature possiedono la issue, `feature_areas` in quale
#                                      gruppo sta la loro `area`. Scriverlo a mano era una copia
#                                      che poteva solo sbagliare — e sbagliava, su 2 nodi su 17,
#                                      perche' il gruppo *sembra* leggibile dal checkpoint
#                                      (`E14.8` suona reazioni) mentre mappa l'`area`.
#                                      Si dichiara **solo** dove non e' derivabile: nessuna
#                                      feature dichiara la issue (4 nodi), la issue attraversa
#                                      piu' domini, oppure il nodo non e' una issue (le sedute).
#                                      Il validator rifiuta un valore che contraddice il registry
#                                      e segnala i nodi che restano senza gruppo.
#   `follows` fra due sedute           lo dichiara gia' `editor-sessions.yaml`, che distingue la
#                                      dipendenza dura (`unblocked_by`) dall'ordine di lavoro
#                                      (`unblocks` senza reciproco, stesso blocco). Riscriverlo
#                                      qui sarebbe la seconda dichiarazione della stessa
#                                      relazione, in un file che l'altro validator non legge.
#
# ## Riferimenti ammessi
#
#   issue:<numero>        una issue GitHub
#   session:U<n>          una seduta di `editor-sessions.yaml`
#   capability:<Nome>     una capability del corpus scenari (`Scenarios/**/*.json` -> `requires`)
#   epic:E<n>             un'epic della roadmap
#
# ## Tipi di relazione
#
#   requires             HARD. `from` deve essere finito prima che `to` possa cominciare.
#   requires_capability  HARD, verso una capability.
#   provides             chi fa esistere una capability.
#   follows              SOFT. Ordine consigliato, mai un blocco. Serve un `rationale`.
#   related              SOFT. Navigazione. Non entra nel calcolo della readiness.
#
# `follows` e `related` non rendono BLOCKED un nodo: e' la regola che questo file esiste per
# rendere verificabile, ed e' pinnata da un test.

schema_version: 1

meta:
  status: THIN_SLICE
  note: >-
    Quattro catene reali invece di tutto il backlog: se non si leggono nel Control Center, non si
    legge nemmeno la v0.1 intera. Le catene A, B e C sono state verificate su GitHub il 2026-08-13;
    la catena D — Mini Autobattle, issue #954-#959 piu' #1069 e #1088 — il 2026-08-16, giorno in cui
    le sei issue di E47 sono nate, e **estesa il 2026-08-21** con i due difetti che bloccano E47.6.
    ⚠️ La riga diceva «Tre catene» con quattro gia' presenti, e la data di verifica valeva per
    un insieme che non conteneva le sei issue nuove: due affermazioni che nessun gate confronta con
    i dati sotto, trovate in code review. Chi aggiunge una catena aggiorna QUESTA riga.

# --- Tassonomia ------------------------------------------------------------------------------

execution_lanes:
  - id: code
    title: CODE
    note: lavoro sul repository, eseguibile da un agente o da chi sviluppa

  - id: pie
    title: PIE
    note: serve una persona davanti all'editor, e l'uscita e' un verdetto

  - id: asset
    title: ASSET
    note: serve una persona davanti all'editor, e l'uscita e' un asset committato

# I raggruppamenti di lettura del lavoro CODE. `feature_areas` mappa le `area` del Feature
# Registry: la copertura e' **totale ed esclusiva** — ogni area sta in esattamente un gruppo — e
# il validator lo verifica, cosi' un'area nuova non finisce in nessun gruppo in silenzio.
#
# Non sono gruppi teorici: la prima stesura ne proponeva nove con aree `Match`, `Presentation`,
# `AI`, `Bot`, `Tooling`, `QA`, `Release` che **nel registry non esistono**, e ometteva
# `Production` e `Gameplay` che esistono. Un gruppo `ai_bots` sarebbe rimasto vuoto: le feature
# del bot hanno `area: Tools`.
domain_groups:
  - id: core_simulation
    title: Core & Simulation
    order: 10
    feature_areas: [Core, Gameplay]

  - id: map_environment
    title: Map & Environment
    order: 20
    feature_areas: [Map, Environment]

  - id: actions_reactions
    title: Actions & Reactions
    order: 30
    feature_areas: [Actions, Reactions]

  - id: perception_network
    title: Perception & Networking
    order: 40
    feature_areas: [Perception, Networking]

  - id: characters_content
    title: Characters & Content
    order: 50
    feature_areas: [Characters, Factions]

  - id: objectives_match
    title: Objectives & Match
    order: 60
    feature_areas: [Objectives]

  - id: ui_presentation
    title: UI & Presentation
    order: 70
    feature_areas: [UI]

  - id: tooling_data_qa
    title: Tooling, Data, QA & Release
    order: 80
    feature_areas: [Tools, Data, Production]

# --- Nodi ------------------------------------------------------------------------------------
#
# `checkpoint:` collega la issue al proprio checkpoint di roadmap, ed e' l'unica fonte di stato
# offline che il generatore ha per un nodo CODE. ⚠️ Vale solo quando l'owner dichiara il glifo:
# `roadmap-v0.1.md` lo scrive per **18 checkpoint su 100**, e per gli altri 82 l'assenza e'
# ambigua — «non cominciato» oppure «chiuso e mai annotato», e per 65 di essi esiste una issue
# GitHub chiusa. Il generatore non prova a distinguerli: marca lo stato `UNKNOWN` e ne dichiara
# la ragione, invece di leggere un default come se fosse una misura.

nodes:

  # --- Catena A · Reactions / Decision Boundary ---------------------------------------------
  #
  # Perche' questa catena: #165 e' un FORK. Sblocca la prosecuzione di E14 e, separatamente,
  # #512 — perche' un DecisionProvider non ha interlocutore finche' nessuno produce una vera
  # opportunity in partita. E' la ragione per cui `DecisionBoundary` merita di essere un nodo e
  # non una falsa dipendenza «E14 dipende da tutta E13».

  - id: issue:164
    release: v0.1
    checkpoint: E14.4

  - id: issue:165
    release: v0.1
    checkpoint: E14.5
    provides:
      - DecisionBoundary

  - id: issue:166
    release: v0.1
    checkpoint: E14.6

  - id: issue:314
    release: v0.1
    domain_group: actions_reactions
    checkpoint: E14.7

  - id: issue:319
    release: v0.1
    checkpoint: E14.8

  - id: issue:512
    release: v0.1
    domain_group: core_simulation
    checkpoint: E15.3

  # --- Catena B · Golden replay / Showcase ---------------------------------------------------
  #
  # Perche' questa catena: #170 e' un JUNCTION — riceve la catena del DecisionProvider,
  # l'obiettivo contestabile e la combo ambientale, da tre domini diversi. E mostra la differenza
  # fra ordine raccomandato e blocco reale: #625 conviene chiuderla prima del pinning del golden,
  # ma non lo blocca.

  - id: issue:66
    release: v0.1
    checkpoint: E8.3

  - id: issue:75
    release: v0.1
    checkpoint: E10.2

  - id: issue:170
    release: v0.1
    checkpoint: E15.4

  - id: issue:171
    release: v0.1
    checkpoint: E15.5
    execution_lane: pie

  - id: issue:625
    release: v0.1

  - id: issue:649
    release: v0.1

  - id: issue:687
    release: v0.1

  # --- Catena C · Character presentation / ASSET / PIE ---------------------------------------
  #
  # Perche' questa catena: e' la sola dove CODE, ASSET e PIE convivono. E mostra che lo stesso
  # allestimento non e' una dipendenza dura — U7/U8/U9 si fanno nella stessa apertura di editor,
  # e `editor-sessions.yaml` lo dichiara gia'.

  - id: issue:287
    release: v0.1
    checkpoint: E21.1

  - id: issue:288
    release: v0.1
    checkpoint: E21.2

  - id: issue:289
    release: v0.1
    checkpoint: E21.3

  - id: issue:593
    release: v0.1
    domain_group: characters_content

  # --- Catena D · Mini Autobattle -----------------------------------------------------------
  #
  # 🔴 **Questo commento diceva che la catena e' «l'unica in cui una dipendenza DURA punta
  # all'indietro», e citava `#512`. Erano due errori in uno**, trovati dallo spec panel su `#512`
  # del 2026-08-16.
  #
  #   (1) IL NUMERO. La prosa della roadmap, del registry e di `scenario-map.md` scriveva **#542**;
  #       questo file scriveva **#512**. Stesso prerequisito, due numeri, stesso commit — e nessun
  #       gate lo vede, perche' entrambe le issue esistono e sono aperte. Sono cose DIVERSE, e
  #       `RTTurnManager.h:603` lo dichiara meglio di quanto lo dichiarasse la roadmap: «non e' il
  #       seam dei `DecisionProvider` di D-101 (#542, v0.2), ne' il `DecisionProvider` iniettabile
  #       di CP 15.3 meta' B (#512). **Sono tre cose in tre release**» — la terza e'
  #       `ARTTurnManager::ReactionDecider`, gia' in `main` da CP 14.5.
  #
  #   (2) LA SPECIE. `requires` qui significa «deve essere finito prima», e **#957 non e' bloccata
  #       da nessuna delle tre**: `PlanBotsForTest()` esiste, e il free-run e' un ciclo sopra di
  #       lui. Cio' che `#542` previene e' il TERZO appiglio ad hoc che D-101 descrive — igiene
  #       architetturale, non un prerequisito. Declassata a `follows` con `rationale`, che e'
  #       esattamente il caso che questo file distingue in testa.
  #
  # Resta vero che `#512` e' un prerequisito DURO di `#170` (golden replay): il golden ha bisogno
  # delle decisioni di finestra come dato, e quell'arco non si tocca.
  #
  # ⚠️ #955 e #956 non hanno una dipendenza tecnica su #954: toccano file diversi — #955 `UI/`,
  # #956 `Map/RTHexMapActor.{h,cpp}` — ed e' il motivo per cui compaiono come `follows` e non come
  # `requires`. *(Fino al 2026-08-20 il vincolo era di ownership fra track del write-set di batch,
  # che non e' topologia del lavoro e viveva altrove; `D-178` ha rimosso quel sistema. L'ordine
  # resta consigliato perche' i tre lavori si sovrappongono, non perche' un lock lo imponga.)*

  - id: issue:954
    release: v0.1
    checkpoint: E47.1

  - id: issue:955
    release: v0.1
    checkpoint: E47.2

  - id: issue:956
    release: v0.1
    checkpoint: E47.3

  - id: issue:957
    release: v0.1
    checkpoint: E47.4

  # Il seam GENERALE di D-101, distinto da `issue:512` che e' quello delle sole finestre.
  # ⚠️ `release: v0.1` perche' lo dichiara il registry, che e' l'owner del campo:
  # `RT-FEAT-TEST-SCENARIO-HARNESS` e' `v0.1` e porta `542` fra le proprie `issues`. Il codice la
  # colloca invece in **v0.2** (`RTTurnManager.h:603`, `RTTestScenario.h:196`): la divergenza e'
  # dichiarata qui invece di essere risolta a intuito, perche' e' una decisione di scope e non un
  # campo da riempire.
  - id: issue:542
    release: v0.1
    domain_group: tooling_data_qa

  # Due gruppi derivabili dal 2026-08-16 — `core_simulation` da RT-FEAT-CORE-DETERMINISM,
  # `objectives_match` da RT-FEAT-MATCH-AUTOBATTLE, perche' il corpus di CP 47.5 e' dichiarato da
  # entrambe. Si sceglie il primo: la domanda del checkpoint e' «la simulazione da' lo stesso risultato
  # nei casi limite», non «quali regole di fine partita valgono» — e il difetto che il corpus ha trovato
  # (#990) sta in `MakeCurrentSnapshot`, cioe' nel core, non nelle condizioni di vittoria.
  - id: issue:958
    release: v0.1
    domain_group: core_simulation
    checkpoint: E47.5

  # Due gruppi derivabili — `objectives_match` da RT-FEAT-MATCH-AUTOBATTLE, `tooling_data_qa` da
  # RT-FEAT-PROD-PACKAGED. Si sceglie il secondo: la domanda del checkpoint e' «la partita gira fuori
  # dall'editor e c'e' l'evidenza», non «quali regole di fine partita valgono».
  - id: issue:959
    release: v0.1
    domain_group: tooling_data_qa
    checkpoint: E47.6

  # ➕ 2026-08-21 — i due difetti che bloccano E47.6 entrano nel grafo. Non sono checkpoint: sono le
  # ragioni per cui il DoD di #959 oggi non e' raggiungibile, e finche' non erano nodi la catena D
  # dichiarava eseguibile un lavoro che non lo e'.
  - id: issue:1088
    release: v0.1
    domain_group: objectives_match

  - id: issue:1069
    release: v0.1
    domain_group: tooling_data_qa

  # ➕ **2026-08-17 — `#833` entra nel grafo.** Decisione d'autore: la issue e' **v0.1** (label `v0.1`,
  # milestone `v0.1 · Mondo giocabile`, e `RT-FEAT-MAP-INTERACTION-GRAPH` passa a `release: v0.1`).
  # Prima non esisteva come nodo, quindi la sua topologia non era verificabile: un documento la
  # affermava in prosa e questo file non la conteneva.
  # ⚠️ Nessun `domain_group`, come `issue:75` e `issue:170`: i quattro gruppi esistenti non coprono il
  # dominio mappa, e inventarne un quinto qui sarebbe un vocabolario nuovo dentro un campo che il
  # `validate` legge.
  # ⛔ **NON ha un arco verso `#170`, e l'assenza e' misurata.** L'handoff che ha motivato questo giro
  # affermava che `#833` «abilita una parte del T5 dello showcase»: e' **falso**. Il gate del turno 5 e'
  # una porta risolta da `EdgeDisabled -> EdgeEnabled` piu' `GraphRevision`, con **CP 9.3 chiuso** —
  # `roadmap-v0.1.md` (*«T5: dopo S2-1, il gate e' una porta»*) e `plans/showcase-v01-audit.md`
  # (*«il meccanismo esiste gia'»*, e l'assertion di T5 e' `S6-1`). T5 e' runnable e non dipende da qui.
  # ✅ **`checkpoint: E23.4` ora e' scrivibile, e la ragione e' una decisione, non un aggiramento.**
  # Il 2026-08-17 questo campo era **assente per decisione del gate**: `feature_registry.py validate`
  # rispondeva `ERROR — checkpoint 'E23.4' che nessun owner dichiara`, perche' **E23 era un'epic v0.2** e
  # nessun owner della v0.1 la dichiarava. L'incoerenza — issue v0.1 con checkpoint in un'epic v0.2 — e'
  # stata risolta all'origine: **`D-160` anticipa E23 intera alla v0.1**, e `roadmap-v0.1.md` §3/§5 ora
  # dichiara i suoi **sette** checkpoint. Il gate ha smesso di rifiutare perche' la premessa e' cambiata,
  # non perche' il campo sia stato forzato.
  # ⛔ **NON ha un arco verso `#170`, e l'assenza e' misurata.** L'handoff che ha motivato questo giro
  # affermava che `#833` «abilita una parte del T5 dello showcase»: e' **falso**. Il gate del turno 5 e'
  # una porta risolta da `EdgeDisabled -> EdgeEnabled` piu' `GraphRevision`, con **E9.3 chiuso** —
  # `roadmap-v0.1.md` (*«T5: dopo S2-1, il gate e' una porta»*) e `plans/showcase-v01-audit.md`
  # (*«il meccanismo esiste gia'»*, assertion `S6-1`). T5 e' runnable e non dipende da qui.
  - id: issue:833
    release: v0.1
    checkpoint: E23.4

  # `#74` entra **solo** perche' l'arco qui sotto ha bisogno di due nodi. E' il consumatore dichiarato
  # di `#833` in `plans/cinque-processi-paralleli-2026-08-17.md` §3: *«#74 CP 10.1 — Activate e Interact
  # sugli oggetti — consuma #833»*. Stessa milestone (`v0.1 · Mondo giocabile`), `P2`.
  - id: issue:74
    release: v0.1
    checkpoint: E10.1

  # Le sedute sono gia' descritte da `editor-sessions.yaml`: qui si dichiara solo che
  # partecipano a questa fetta e in quale dominio si leggono. Lane e stato arrivano da la'.
  - id: session:U7
    release: v0.1
    domain_group: characters_content

  - id: session:U8
    release: v0.1
    domain_group: characters_content

  - id: session:U9
    release: v0.1
    domain_group: characters_content

# --- Relazioni -------------------------------------------------------------------------------

relations:

  # Hard — «prima questo, o l'altro non puo' cominciare»
  - from: issue:164
    to: issue:165
    type: requires

  - from: issue:165
    to: issue:166
    type: requires

  - from: issue:165
    to: issue:314
    type: requires

  - from: issue:165
    to: issue:512
    type: requires

  - from: issue:512
    to: issue:170
    type: requires

  - from: issue:66
    to: issue:170
    type: requires

  - from: issue:75
    to: issue:170
    type: requires

  - from: issue:170
    to: issue:171
    type: requires

  - from: issue:287
    to: issue:288
    type: requires

  - from: issue:287
    to: issue:289
    type: requires

  - from: issue:954
    to: issue:959
    type: requires

  - from: issue:956
    to: issue:959
    type: requires

  # ➕ 2026-08-21 — le due dipendenze che decidono se E47.6 e' ESEGUIBILE, e che non erano nel grafo.
  # Il corpo di #959 elencava «E47.1, E47.3» e si fermava li': nessuna delle due e' la ragione per cui
  # oggi non c'e' niente da registrare.
  - from: issue:1088
    to: issue:959
    type: requires
    rationale: >-
      Il DoD di #959 chiede una partita «dall'avvio alla vittoria». #1088 misura un **pareggio 12/12
      con zero eventi Combat** sulla configurazione spedita, riprodotto su DUE arene diverse (64 e 65
      celle). Finche' resta aperta, l'esecuzione di #959 produce l'evidenza di un pareggio: e' un
      blocco sul merito, non un ordine di lavoro.

  - from: issue:1069
    to: issue:959
    type: requires
    rationale: >-
      Il DoD di #959 chiede una mappa esagonale multilivello e dichiara «non sull'arena generata di
      test: e' precisamente la riserva che G13 dichiara». #1069 e' il `BP_GameMode` spedito con
      `MapSource = GeneratedTestArena`, cioe' proprio quella. Registrare senza chiuderla riprodurrebbe
      la riserva invece di toglierla, e G13 resterebbe 🟡.

  - from: issue:955
    to: issue:958
    type: requires

  - from: issue:957
    to: issue:958
    type: requires

  # Capability — un provider e sei consumatori
  #
  # `DecisionBoundary` non e' un nome nuovo: e' la capability che gli scenari del corpus dichiarano
  # in `requires`. 🔴 **Questo commento diceva «SETTE scenari … e `RTScenarioSession.cpp` non la
  # elenca fra le disponibili. Sono quindi BLOCKED»: dal 2026-08-16 e' falso in ENTRAMBE le meta',
  # e la vista generata lo diceva mentre la sua sorgente no** — che e' il verso peggiore, perche'
  # l'autorevole e' questo file. Rimisurato sul corpus, non ricopiato:
  #
  #   · **sei** consumatori, non sette: `Spec.Brace.ProfileChangesResponse` e' passato a
  #     `ReactionProfile` (`#512` fase B), perche' il suo blocco vero e' il profilo a due risposte —
  #     senza, quel turno si sbloccava e passava con `intents: []`;
  #   · la capability **e' fra le disponibili** (`#512` fase B, PR #1033): il provider e' chiuso e
  #     `AskReactionDecision` ha un chiamante di produzione;
  #   · dei sei, **due girano** (`RT_Showcase_Relay_v01` T4, `Spec.Overwatch.HoldThenFire` T2) e
  #     **quattro restano BLOCKED** — i `Spec/Clash/*`, che chiedono anche `ReactionClash`. Scoprire
  #     la finestra non scopre la scelta contesa.
  #
  # L'arco `provides` resta: chi ha fornito la capability e' `#165`, e la provenienza non cambia
  # perche' la capability e' atterrata.
  - from: issue:165
    to: capability:DecisionBoundary
    type: provides

  - from: capability:DecisionBoundary
    to: issue:512
    type: requires_capability

  # Soft — ordine, non blocco
  - from: issue:166
    to: issue:314
    type: follows
    rationale: CP 14.7 dichiara di seguire CP 14.6.

  - from: issue:314
    to: issue:319
    type: follows
    rationale: CP 14.8 segue CP 14.7, e dichiara di non precedere 14.5/14.6.

  - from: issue:625
    to: issue:170
    type: follows
    rationale: >-
      Chiudere la traccia del danno da hazard prima del pinning del golden evita una
      rebaseline. E' un risparmio, non un prerequisito.

  # Mini Autobattle — il seam di D-101 PRECEDE il free-run, non lo blocca.
  - from: issue:542
    to: issue:957
    type: follows
    rationale: >-
      Igiene architetturale, non un prerequisito tecnico: `PlanBotsForTest()` esiste gia' e il free-run
      e' un ciclo sopra di lui, quindi `#957` e' apribile oggi. Cio' che `#542` previene e' il TERZO
      appiglio ad hoc all'harness che D-101 descrive — aprirlo dopo costa una riscrittura, aprirlo
      prima costa un'attesa. 🔴 Questo arco era `issue:512 --requires--> issue:957`: sbagliato nel
      numero (la prosa diceva #542) e nella specie (`requires` = blocco). Spec panel del 2026-08-16.

  - from: issue:954
    to: issue:955
    type: follows
    rationale: >-
      Nessuna dipendenza tecnica. La ragione originale era un vincolo di ownership del write-set
      (`Source/RefactorTactics/UI/` alla track `client_tools`, #78) ed e' **decaduta con D-178**:
      l'ordine resta un risparmio, non un prerequisito. Entrambe le issue sono chiuse.

  - from: issue:954
    to: issue:956
    type: follows
    rationale: >-
      Nessuna dipendenza tecnica. La ragione originale era un vincolo di ownership su un'ALTRA track
      (`Map/RTHexMapActor.{h,cpp}` a `content_editor`, #451) ed e' **decaduta con D-178**: il write-set
      di batch non esiste piu' e #451 e' chiusa. Resta vero cio' che il rationale diceva a lato: la
      board si giudica su una partita che gira da sola, quindi l'ordine e' un risparmio.

  # Related — navigazione, mai readiness
  - from: issue:593
    to: session:U7
    type: related
    rationale: La issue dichiara esplicitamente che il workaround regge e che non blocca U7.

  - from: issue:593
    to: session:U9
    type: related
    rationale: Influenza il giudizio visivo di U9 senza esserne un prerequisito dichiarato.

  - from: issue:649
    to: issue:170
    type: related
    rationale: Consistency del TurnLog; nessuna dipendenza dura dichiarata verso CP 15.4.

  - from: issue:687
    to: issue:170
    type: related
    rationale: >-
      Rischio di versioning e migrazione del formato. Finche' un owner non lo rende un gate,
      resta una relazione di consistenza.

  # Evidenza — chi realizza e chi giudica
  # ➕ 2026-08-17 — l'unico arco che le prove sostengono per `#833`. Il grafo sorgente -> bersaglio e' il
  # **dato** che `#74` consuma: senza la relazione, `Activate`/`Interact` su un oggetto non ha come
  # nominare il proprio bersaglio. Provenienza della dipendenza: `plans/cinque-processi-paralleli-2026-08-17.md`
  # §3, che dichiara *«#74 ... consuma #833»*. Per l'arco che NON esiste — verso `#170` — vedi il
  # commento del nodo `issue:833`.
  - from: issue:833
    to: issue:74
    type: requires
    rationale: >-
      `#74` (CP 10.1, `Activate`/`Interact` sugli oggetti) consuma il grafo sorgente -> bersaglio che
      `#833` (CP 23.4) dichiara come dato. La direzione e' quella misurata sul motore: `SetDoorState`
      esiste e restituisce i cambi realmente avvenuti (`D-150`), ma nessuna azione di catalogo lo
      dichiara — `git grep SetDoorState -- Source/RefactorTactics/Ability/` da' **zero** — quindi il
      consumatore ha bisogno del dato, non il contrario.

  - from: session:U7
    to: issue:287
    type: implements

  - from: session:U8
    to: issue:288
    type: implements

  - from: session:U9
    to: issue:289
    type: verifies
